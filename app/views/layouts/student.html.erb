<!-- app/views/layouts/student.html.erb -->
<!DOCTYPE html>
<html>
  <head>
    <title>Student Portal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-[#f8fafc] min-h-screen">
    <%= render "shared/flash" %>
    <header class="bg-white border-b border-slate-200 px-8 py-4">
      <div class="flex justify-between items-center mx-auto">
        <div>
          <h1 class="text-xl font-bold">Student Portal</h1>
          <p class="text-sm text-slate-400"><%= current_user.name %> (<%= current_user.student_id %>)</p>
        </div>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, 
            class: "px-4 py-2 bg-[#261ce6] hover:bg-[#1a14a3] text-white text-sm font-semibold rounded-lg transition cursor-pointer" %>
      </div>
    </header>
    <main class="">
      <%= yield %>
    </main>
  </body>
</html>
