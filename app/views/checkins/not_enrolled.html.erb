<!-- app/views/checkins/not_enrolled.html.erb -->
<div class="bg-white rounded shadow p-6 w-full max-w-md text-center">
  <div class="text-6xl mb-4">🚫</div>
  <h1 class="text-2xl font-bold mb-2 text-red-600">ไม่มีสิทธิ์เช็คชื่อ</h1>
  <p class="text-slate-500 mb-4"><%= @course.code %> - <%= @course.name %></p>
  <p class="text-slate-500">รหัส <%= current_user.student_id %> ไม่ได้ลงทะเบียนในวิชานี้</p>
</div>
