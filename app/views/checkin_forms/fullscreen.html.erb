<!DOCTYPE html>
<html>
  <head>
    <title>QR Code - <%= @checkin_form.title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-[#f8fafc] p-10"
        data-controller="qr-refresh"
        data-qr-refresh-url-value="<%= qr_code_course_checkin_form_path(@course, @checkin_form) %>">
    <div class="bg-white shadow-xl rounded-3xl px-16 py-12 text-center max-w-4xl w-full">
      <h1 class="text-3xl font-bold tracking-tight text-slate-900 mb-3">
        <%= @course.code %>
      </h1>
      <h2 class="text-2xl text-slate-600 mb-2">
        <%= @course.name %>
      </h2>
      <p class="text-lg text-slate-500 mb-10">
        <%= @checkin_form.title %>
      </p>
      <div class="mb-10 flex justify-center"
           data-qr-refresh-target="qrcode">
        <div class="w-[420px] max-w-full">
          <%= @checkin_form.qr_code_svg(@host).html_safe %>
        </div>
      </div>
      <p class="text-xl font-medium text-slate-700 mb-4">
        สแกน QR เพื่อเช็คชื่อเข้าเรียน
      </p>
      <p class="text-sm text-slate-400 break-all"
         data-qr-refresh-target="url">
        <%= @checkin_form.checkin_url(@host) %>
      </p>
    </div>
  </body>
</html>
